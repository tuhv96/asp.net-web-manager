@using managermentSmartphone.Models;
@model List<SmartPhone>
<link href="~/Content/HomeCss.css" rel="stylesheet" />


@{
    Layout = "~/Views/layout/userLayout.cshtml";
    ViewBag.Title = "Home";
}
@*div body page index*@
<div id="divbody">
        <div id="ListPrd"><h1>Us Products</h1></div>
    @{int i = 0;}
    @foreach (var item in Model)
        {
            i++;
            <div id="Products">
                <div id="img"><img src="@Url.Content("~/img/"+item.imageSmartphone)" width="200" height="250"></div>
                <div id="information">
                    @*nếu tên phone dài hơn 30 kí tự lấy từ kí tự 0 -> 30*@

                    @*muon hien thi bao nhieu san pham thi tan hay giam so lan i muon chia*@
                    @if (item.nameSmartphone.Length < 30)
                    {
                        <div id="namephone" title="@item.nameSmartphone">@item.nameSmartphone</div>
                    }
                    else
                    {
                        <div id="namephone" title="@item.nameSmartphone">@item.nameSmartphone.Substring(0, 30)</div>
                    }
                    <div id="price" style="color:orange"><font color="green"></font> @String.Format("Price: {0:0,00} vnđ",item.price);</div>
                </div>
            </div>
                if (i % 2 == 0)
                {
                    <div style="clear:both"></div>
                }

        }
</div>
<div style="clear:both"></div>
@section detail{
    <div>
        The innovation beneath 3D Touch.
        Using 3D Touch to do more with your iPhone is as simple as pressing the display. But what’s happening underneath the surface is a sophisticated process. To provide the nuanced effect of force being gradually applied and removed, 3D Touch detects a dynamic range of pressure to a microscopic degree. It even accounts for changes in gravity as you hold your phone
    </div>
}